sequences:
  - protein:
      id: A
      sequence: null
      msa: null
  - ligand:
      id: B
      smiles: null
templates:
  - cif: null
    force: true # optional, if force is set to true (default is false), a potential will be used to enforce the template
    threshold: 2.0 # optional, controls the distance (in Angstroms) that the prediction can deviate from the template
    chain_id: [A] # can be more than one
    template_id: [A]
properties:
  - affinity:
      binder: B
